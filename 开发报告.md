# NetSentry (网哨) 软件开发报告

## 1. 项目概述

| 项目 | 内容 |
|------|------|
| 软件名称 | NetSentry (网哨) |
| 版本 | 0.1.1 |
| 开发者 | MagicianEW |
| 技术栈 | Tauri v2 + Rust + TypeScript + Vite |
| 目标平台 | Windows |
| 源码仓库 | https://github.com/MagicianEW/netsentry |
| 发布地址 | https://github.com/MagicianEW/netsentry/releases |

---

## 2. 软件需求规格

### 2.1 需求来源

根据用户需求文档，NetSentry (网哨) 是一款运行在 Windows 系统上的网络监控工具。

### 2.2 功能需求

| 需求编号 | 功能描述 | 实现状态 |
|----------|----------|----------|
| F001 | 系统托盘运行，图标显示在系统托盘区域 | ✅ 已实现 |
| F002 | 右键单击托盘图标弹出菜单 | ✅ 已实现 |
| F003 | 托盘菜单包含：语言切换、启动项设置、端口设置 | ✅ 已实现 |
| F004 | 提供 Web 界面，可通过浏览器访问 | ✅ 已实现 |
| F005 | Web 界面支持响应式布局（手机版/电脑版） | ✅ 已实现 |
| F006 | 实时监测互联网连接状态 | ✅ 已实现 |
| F007 | 检测方式：ping 指定服务器 + 网络适配器状态双重检测 | ✅ 已实现 |
| F008 | 发现断网后在界面上显示警告 | ✅ 已实现 |
| F009 | 断网时记录时间到日志 | ✅ 已实现 |
| F010 | 软件语言设置为中文时，界面和日志显示中文 | ✅ 已实现 |
| F011 | 软件语言设置为英文时，界面和日志显示英文 | ✅ 已实现 |
| F012 | 日志文件使用英文记录，保证兼容性 | ✅ 已实现 |
| F013 | 日志文件大小限制 128KB，超限后归档 | ✅ 已实现 |
| F014 | 归档日志命名为日期时间格式 | ✅ 已实现 |
| F015 | 支持公网 IP 用户通过端口映射远程访问 | ✅ 已实现 |
| F016 | 开机启动功能 | ✅ 已实现 |

### 2.3 非功能需求

| 需求编号 | 需求描述 | 实现状态 |
|----------|----------|----------|
| N001 | 软件安装包格式：NSIS (.exe) 和 MSI | ✅ 已实现 |
| N002 | 托盘图标不显示额外命令行窗口 | ✅ 已实现 |
| N003 | 软件名称：中文"网哨"，英文"NetSentry" | ✅ 已实现 |

---

## 3. 技术架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    NetSentry (网哨)                      │
├─────────────────────────────────────────────────────────┤
│  前端层 (TypeScript + Vite)                              │
│  ├── 响应式 Web UI (手机/电脑自适应)                      │
│  ├── 中英文国际化                                         │
│  └── 实时状态展示与告警                                   │
├─────────────────────────────────────────────────────────┤
│  后端层 (Rust + Tauri)                                   │
│  ├── 系统托盘管理                                         │
│  ├── 网络状态监测 (ping + 适配器)                         │
│  ├── 日志管理 (轮转/归档)                                 │
│  ├── Web 服务器 (Axum)                                   │
│  └── 设置持久化                                           │
├─────────────────────────────────────────────────────────┤
│  基础设施                                                 │
│  ├── Windows Registry (开机启动)                         │
│  └── 文件系统 (配置/日志存储)                             │
└─────────────────────────────────────────────────────────┘
```

### 3.2 核心技术选型

| 组件 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 框架 | Tauri | v2 | 桌面应用框架 |
| 后端语言 | Rust | 1.96+ | 高性能后端逻辑 |
| 前端语言 | TypeScript | ~6.0 | 前端开发 |
| 前端框架 | Vanilla TS | - | 轻量级前端 |
| 构建工具 | Vite | 8.x | 前端打包 |
| Web 服务器 | Axum | 0.8 | HTTP 服务 |
| 日志库 | simplelog | 0.12 | 日志记录 |
| 时间库 | chrono | 0.4 | 时间处理 |
| 网络接口 | network-interface | 2 | 网卡检测 |

### 3.3 项目结构

```
netsentry/
├── .github/
│   └── workflows/
│       └── build.yml           # GitHub Actions 构建流程
├── src/                        # 前端源码
│   ├── main.ts                 # 前端逻辑 (TypeScript)
│   └── styles.css              # 响应式样式
├── src-tauri/                  # Tauri 后端源码
│   ├── src/
│   │   ├── main.rs             # 程序入口
│   │   └── lib.rs              # 核心逻辑
│   ├── Cargo.toml              # Rust 依赖配置
│   ├── tauri.conf.json         # Tauri 配置
│   ├── capabilities/
│   │   └── default.json        # 权限配置
│   └── icons/                  # 应用图标
├── index.html                  # 前端入口页面
├── package.json                # Node.js 依赖
├── tsconfig.json               # TypeScript 配置
├── vite.config.ts              # Vite 配置
└── README.md                   # 项目说明
```

---

## 4. 功能详细设计

### 4.1 系统托盘

**实现方式：**
- 使用 Tauri v2 的 `TrayIconBuilder` 动态创建托盘图标
- 移除了 `tauri.conf.json` 中的静态托盘配置，避免双图标问题

**托盘菜单结构：**
```
├── Settings (设置)
├── View Logs (查看日志)
├── ─────────────
├── 中文 (切换为中文)
├── English (Switch to English)
├── ─────────────
├── About (关于)
└── Quit (退出)
```

**关键代码：**
```rust
let _tray = TrayIconBuilder::new()
    .menu(&menu)
    .tooltip("NetSentry")
    .icon(app.default_window_icon().unwrap().clone())
    .menu_on_left_click(false)
    .on_menu_event(|app, event| { ... })
    .build(app)?;
```

### 4.2 网络状态监测

**检测机制：**
1. **适配器状态检测**：遍历系统网络接口，检查 Ethernet/Wi-Fi 接口是否有有效 IP 地址
2. **Ping 检测**：向多个目标服务器 (8.8.8.8, 1.1.1.1, 114.114.114.114) 发送 ICMP 请求
3. **综合判断**：两个检测都通过才认为在线

**检测间隔：** 5 秒

**关键代码：**
```rust
fn check_adapter_status() -> bool {
    // 遍历网络接口，检查 Ethernet/Wi-Fi
    for iface in interfaces {
        let name_lower = iface.name.to_lowercase();
        if name_lower.contains("ethernet") || name_lower.contains("wi-fi") {
            if !iface.addr.is_empty() {
                return true;
            }
        }
    }
    false
}

fn check_ping() -> bool {
    for target in PING_TARGETS {
        let output = Command::new("ping")
            .args(["-n", "1", "-w", "1000", target])
            .creation_flags(0x08000000) // CREATE_NO_WINDOW
            .output();
        if output.status.success() {
            return true;
        }
    }
    false
}
```

### 4.3 日志管理

**日志规格：**
- 文件名：`netsentry.log`
- 位置：`%LOCALAPPDATA%\NetSentry\logs\`
- 大小限制：128KB
- 编码：英文（保证兼容性）

**轮转机制：**
- 当日志文件超过 128KB 时
- 当前日志文件重命名为 `netsentry_YYYYMMDD_HHMMSS.log`
- 创建新的 `netsentry.log` 继续记录

**日志格式：**
```
[2026-09-11 12:34:56.789] [INFO] [ZH] Network disconnected
[2026-09-11 12:35:01.234] [INFO] [ZH] Network restored
```

**国际化显示：**
- 软件内查看日志时，根据当前语言设置显示对应翻译
- 日志文件本身始终使用英文记录

### 4.4 Web 服务器

**实现方式：**
- 使用 Rust 的 Axum 框架
- 在独立线程中运行，不阻塞主线程

**API 端点：**

| 端点 | 方法 | 描述 |
|------|------|------|
| `/` | GET | 返回 Web UI 页面 |
| `/api/status` | GET | 获取网络状态 |
| `/api/settings` | GET | 获取设置 |
| `/api/save-settings` | POST | 保存设置 |
| `/api/logs` | GET | 获取日志内容 |

**配置方式：**
- 端口在设置中可配置，默认 8080
- 绑定地址：`0.0.0.0`（允许外部访问）

### 4.5 开机启动

**实现方式：**
- Windows Registry 操作
- 注册表路径：`HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Run`
- 键名：`NetSentry`

**关键代码：**
```rust
#[cfg(target_os = "windows")]
fn set_autostart(enable: bool) -> Result<(), String> {
    let key_path = r"SOFTWARE\Microsoft\Windows\CurrentVersion\Run";
    let reg_key = RegKey::predef(HKEY_CURRENT_USER)
        .open_subkey_with_flags(key_path, KEY_SET_VALUE)?;

    if enable {
        reg_key.set_value("NetSentry", &exe_path)?;
    } else {
        let _ = reg_key.delete_value("NetSentry");
    }
    Ok(())
}
```

### 4.6 国际化

**支持语言：**
- 中文 (zh)
- 英文 (en)

**翻译内容：**
- 界面所有文本
- 日志显示内容

**实现方式：**
- 使用 HashMap 存储翻译字典
- 根据设置的语言动态切换

---

## 5. 配置与数据存储

### 5.1 配置文件

**位置：** `%APPDATA%\NetSentry\settings.json`

**格式：**
```json
{
  "language": "zh",
  "port": 8080,
  "autostart": false
}
```

### 5.2 日志目录

**位置：** `%LOCALAPPDATA%\NetSentry\logs\`

**文件：**
- `netsentry.log` - 当前日志
- `netsentry_YYYYMMDD_HHMMSS.log` - 归档日志

---

## 6. 构建与发布

### 6.1 构建方式

**本地构建：**
```bash
npm install
npm run tauri build
```

**GitHub Actions 自动构建：**
- 触发条件：push 到 master 分支
- 构建平台：windows-latest
- 输出：NSIS 安装包 + MSI 安装包

### 6.2 构建产物

| 文件名 | 格式 | 说明 |
|--------|------|------|
| NetSentry_0.1.1_x64-setup.exe | NSIS | 安装向导 |
| NetSentry_0.1.1_x64_en-US.msi | MSI | Windows Installer |

### 6.3 发布流程

1. 代码 push 到 master 分支
2. GitHub Actions 自动构建
3. 构建成功后自动创建/更新 Release
4. 下载地址：https://github.com/MagicianEW/netsentry/releases

---

## 7. Git 版本管理

### 7.1 提交记录

| 哈希 | 说明 |
|------|------|
| 6f66e78 | Initial NetWatcher implementation |
| d21efc6 | Rename to NetSentry (网哨) v0.1 |
| 5dbd94f | Fix Rust compilation errors |
| d8973c1 | Fix write_log String vs &str, state.clone() vs Arc |
| 88e33b7 | Fix Arc clone: separate clones for each closure |
| 7197d5b | Use state_for_thread directly in closures |
| 0aaae59 | Add Windows CommandExt trait for creation_flags |
| 777ecb5 | Fix duplicate tray icon and settings menu item |
| 22a5c79 | Add development report |
| 6975bf4 | v0.1.1: 版本号单一来源改造、隐私信息清理、作者改为 MagicianEW |

### 7.2 分支策略

- 主分支：`master`
- 保护策略：所有更改通过 PR 合并

---

## 8. 已知问题与限制

| 问题 | 状态 | 说明 |
|------|------|------|
| 双托盘图标 | 🟡 已修复 | 移除静态托盘配置，等待验证 |
| 命令行窗口闪现 | 🟡 已修复 | ping 命令添加 CREATE_NO_WINDOW，等待验证 |
| 仅支持 Windows | ℹ️ 设计如此 | 需求限定 Windows 平台 |

---

## 9. 未来改进建议

1. **增加更多语言支持** - 如日语、韩语等
2. **历史统计图表** - 展示断网频率和时长的可视化图表
3. **通知中心集成** - Windows 通知替代界面警告
4. **远程管理** - 通过 Web API 进行远程配置
5. **自动更新** - 集成 Tauri 的内置更新功能

---

## 10. 附录

### 10.1 参考资料

- [Tauri v2 文档](https://tauri.app/)
- [Rust 官方文档](https://doc.rust-lang.org/)
- [Axum 框架](https://docs.rs/axum/)
- [Chrono 时间库](https://docs.rs/chrono/)

### 10.2 许可证

本项目为开源项目，具体许可证见 GitHub 仓库。

---

**报告生成时间：** 2026-09-11
**报告版本：** 1.0
